# Azure AD application (client) ID of your app registration.
MS_CLIENT_ID=your-ms-client-id

# Optional. "common" (default) supports both personal Microsoft accounts and
# work/school accounts. Set to a specific tenant ID to restrict sign-in.
# MS_TENANT_ID=common
